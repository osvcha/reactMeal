import {StyleSheet} from 'react-native';
import colors from '../../../assets/colors';

export const loadingColor = colors.grey;
const styles = StyleSheet.create({
  container: {backgroundColor: colors.main, flex: 1},
});

export default styles;
